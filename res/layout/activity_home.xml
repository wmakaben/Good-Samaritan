<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@drawable/cloud_background"
    tools:context=".HomeActivity" >

    <Button
        android:id="@+id/lend_hand_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:text="@string/lend_a_hand"
        android:onClick="lendAHand"
        android:textSize="20sp"
        android:textStyle="bold" />

    <Button
        android:id="@+id/help_button"
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:background="@drawable/the_glowdilocks"
        android:onClick="requestHelp" />

    <!-- Invisible button to put on top of the help button image. It narrows down the space that can be clicked -->
    <!--
    <Button
        android:id="@+id/hidden_help_button"
        android:layout_width="235dp"
        android:layout_height="235dp"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:background="@drawable/big_round_button"
        android:onClick="requestHelp" />
    -->
    
    <Button
        android:id="@+id/pending_requests_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:text="@string/pending_requests"
        android:textSize="20sp"
        android:textStyle="bold"
        android:onClick="showPendingRequests" />

</RelativeLayout>
